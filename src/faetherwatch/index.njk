---
title: Faetherwatch Family Archive
layout: blog
family: faetherwatch
audioSlug: 'steampunk-instrumental-v2.m4a'
audioTitle: 'Unknown Composer - Faetherwatch Family Song'
portraitSlug: 'rusty-compressed.jpg'
portraitCaption: 'Rhodon “Rusty” Faetherwatch (2998-3086), Treasurer of Rivet River. His life was cut tragically short after he choked on some particularly spicy stew at the tender age of 88.'
---

<section class="mt-16">
	{% set postslist = collections.faetherwatch %}

	{% for post in postslist %}
	<article class="my-10 postlist">
		<header class="block mb-4">

			<h2 class="font-bold text-3xl text-dirt-700 hover:text-dirt-900 opacity-70 mb-2">
				<a href="{{ post.url | url }}">
					{% if post.data.title %}
					{{ post.data.title }}
					{% else %}
					Untitled
					{% endif %}
				</a>
			</h2>
			{% if post.data.subtitle %}
			<p class="font-serif text-lg text-dirt-800">
				{{ post.data.subtitle }}
			</p>
			{% endif %}
		
		</header>
		<p class="text-sm leading-6 mb-1 opacity-90 font-bold">
			<time>{{ post.date | readableDate }}</time>
		</p>
		<p class="font-serif">by <span class="">{{ post.data.author }}</span></p>
		
		<p class="font-serif opacity-75 mt-6 lg:mt-8">
			{% excerpt post %}
		</p>

		<p class="py-2">
			<a href="{{ post.url | url }}" class="text-dirt-700 hover:text-dirt-900 font-bold font-faetherwatchTitle">
				Read more →
			</a>
		</p>
	<hr class="inline-block w-full">
	</article>

	{% endfor %}
</section>
